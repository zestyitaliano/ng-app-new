import type { ToolMeta } from "../types";

export const toolMeta: ToolMeta = {
  slug: "image-to-text",
  title: "Image to Text",
  description: "Extract text from images locally in your browser (no uploads).",
  category: "Images",
} satisfies ToolMeta;
